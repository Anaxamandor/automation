"""the new way of formating strings""

device_name = "R1"
version_number = 15.8

print(f"The device name is {device_name} and the version number is {version_number}")

#fstrings automatically convert integers to strings
