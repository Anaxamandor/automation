### password rotation on encrypted files ###
ansible-vault rekey --help

#changing the password (key) on an encrypted file:
ansible-vault rekey --valt-password-file password.sh secret.yml
or
ansible-vault rekey secret.yml

# if using vault ids: 
all encrypted files with the same ID need to have a matching key, so rekey all files with the same ID at the same time
